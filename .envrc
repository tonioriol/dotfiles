# .envrc - Environment variables for this directory
# This file is loaded automatically by direnv when you cd into this directory
# After editing, run: direnv allow

# Example: Use mise to manage tool versions globally
# mise is great for managing global tool versions (node, python, etc.)
# Uncomment the line below to automatically load mise configuration
# use mise

# Example: Use devbox for project-specific reproducible environments
# devbox creates isolated environments with Nix (includes system packages, not just language runtimes)
# Use devbox when you need full reproducibility including system dependencies
# Use mise for simpler language version management
# Uncomment the line below to automatically load devbox configuration
# use devbox

# Example: Set environment variables
# export PROJECT_NAME="dotfiles"
# export ENV="development"

# Example: Load secrets from a file (make sure to add it to .gitignore)
# source_env_if_exists .envrc.local

# Example: Add a directory to PATH
# PATH_add bin

# Example: Load .env file if it exists
# dotenv_if_exists